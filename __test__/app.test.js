import { handleSubmit } from "../src/client/js/app";

describe('Check Handlesubmit function', () => {
    test('Check handlesubmit function', () => {
        expect(handleSubmit).toBeDefined();
    });
});